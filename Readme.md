### Run:  `npm install` 
- after runing `npm install` following package would be installed
- `"body-parser" ,
    "cors" ,
    "express" ,
    "joi",
    "mongoose" `

### To run project
- Run:  `npm start`
- Or
- Run: `node app.js`